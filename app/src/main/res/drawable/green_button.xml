<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 백그라운드 색상 -->
    <solid android:color="#007B4F"/>

    <!-- 모서리 둥글기 -->
    <corners android:radius="10dp"/>

    <!-- 테두리 설정 (선택적) -->
<!--    <stroke-->
<!--        android:width="2dp"-->
<!--        android:color="#007AFF"/>-->

    <!-- 패딩 (선택적) -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/>

    <!-- 그림자 (선택적) -->
    <size
        android:width="250dp"
        android:height="100dp"/>
</shape>
